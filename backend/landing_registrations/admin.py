# from django.contrib import admin
# from .models import LandingPageRegistration

# MongoEngine documents cannot be registered with Django Admin directly.
# Admin UI logic for LandingPageRegistration is now handled via the React frontend.
